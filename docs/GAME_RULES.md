# KPL Link 游戏规则

- 玩家从系统给出的起点和终点之间添加职业选手。
- 每一段必须是有赛季证据的直接队友关系。
- 有效路径的跳数等于节点数减一。
- 最短答案按“路径有效且跳数等于 BFS 最短距离”判断，因此多条同长度最短路径都能获胜。
- 无效提交会标出具体断裂段；有效但较长的提交允许继续修改，不自动泄露答案。
- 只有玩家主动点击“查看最短路径”才展示一条 BFS 最短路径。
- 入门抽取距离 1–2，标准抽取距离 2，困难抽取距离 3；考古仅在真实图中存在距离 4 及以上时显示。
